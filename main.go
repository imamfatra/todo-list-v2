package main

import (
	_ "github.com/lib/pq"
)

func main() {
	// var query *repository.Queries

	// config, err := model.LoadConfig("./")
	// if err != nil {
	// 	log.Fatal("cannot load config: ", err)
	// }

	// conn, err := sql.Open(config.DBDriver, config.DBSource)
	// if err != nil {
	// 	log.Fatal("cannot connect to db: ", err)
	// }

	// query = repository.New(conn)

}
